There is no 'common' folder in the root folder of the app.

The common files are used on files that are shared by one more application.

For instance 'middlewares/errorHandler.js' is used in the all apps which had been named as app, api and static.
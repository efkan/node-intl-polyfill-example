/*
 * Common is used for to provide the sharing, common data.
 * For instance middlewares/errorHander.js is a common file.
 * When seperating the app to different servers then common
 * files can be copied to under each app folder like
 * app/middlewares/errorHander.js
 ***********************************************************/

module.exports = {
  // middlewares: require('./middlewares'),
}

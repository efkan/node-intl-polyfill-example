/*
 * That index file exposes the Translations.
 * Some translations need to be formatted according to a given parameter
 * by using Intl.MessageFormat
 *
 */
module.exports = {
	api: require('./api'),
  app: require('./app'),
	static: require('./static'),
	common: require('./common'),
}
